language: java

os: linux
dist: trusty
jdk: oraclejdk8

install:
  - git clone https://github.com/openjdk/jmc.git
  - mvn p2:site -f jmc/releng/third-party/pom.xml
  - mvn jetty:run -f $JMC_THIRD_PARTY/pom.xml &
  - mvn install --quiet -f -DskipTests=true -Dspotbugs.skip=true jmc/core/pom.xml
  - mvn package --quiet -f -DskipTests=true -Dspotbugs.skip=true jmc/pom.xml

script:
  - ./gradlew shadowJar

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.m2